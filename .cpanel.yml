---
deployment:
  tasks:
    - export DJANGO_SETTINGS_MODULE=teamtasks.settings
    - /opt/venv/bin/pip install -r requirements.txt
    - python manage.py migrate
    - python manage.py collectstatic --noinput
    - python manage.py runserver 0.0.0.0:8000
